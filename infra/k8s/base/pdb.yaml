# A node drain may not take the last api pod with it. Without this, a cluster
# upgrade can evict both replicas at once.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api
