apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ENV: staging
  API_PREFIX: /api/v1
  LLM_PROVIDER: mock
  VECTOR_BACKEND: pgvector
  EMBEDDING_MODEL: sentence-transformers/all-MiniLM-L6-v2
  S3_BUCKET: scih-documents
  CORS_ORIGINS: '["http://localhost"]'
