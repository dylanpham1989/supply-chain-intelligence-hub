apiVersion: v1
kind: ServiceAccount
metadata:
  name: scih
  annotations:
    # IRSA. The pod exchanges its projected service account token for temporary
    # IAM credentials, so S3 access needs no access key stored anywhere.
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/scih-staging-app
