apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: scih
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - scih-staging.example.com
      secretName: scih-tls
  rules:
    - host: scih-staging.example.com
