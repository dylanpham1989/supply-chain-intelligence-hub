apiVersion: 1

datasources:
  - name: Prometheus
    # Pinned: the dashboard JSON refers to this uid, and Grafana would
    # otherwise generate one and leave every panel without a datasource.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
